import { createServer } from 'node:http'

const port = process.env.PORT || '{{ .Port }}'

createServer((req, res) => {
  if (req.url === '/healthz') {
    res.writeHead(200).end('ok\n')
    return
  }
  res.writeHead(200).end('{{ .Name }}\n')
}).listen(port, '0.0.0.0', () => {
  console.log(JSON.stringify({ msg: 'started', addr: ':' + port }))
})
