v1.3.2

v1.3.2 spec tagging is idempotent, so a failed build no longer skips a release

v1.3.1 generated clients re-export their schema types; duration_ms is not truncated

v1.3.0 vite bundles the image; hot reload for services

v1.2.0 node-service is TypeScript, run directly with no build step

v1.1.13 regen keeps the module path and the owner's real casing

v1.1.12 generated clients carry repository.url, which --provenance requires

v1.1.11 one publish.yaml per repo; init prints the npm trust command

v1.1.10 regen syncs the TypeScript client to the spec version; go-tui runtime

v1.1.9 Argo reads each service from its own repo; render --register opens the PR
